package com.growcn.mp3player01.service;import android.app.Service;import android.content.Intent;import android.os.IBinder;import android.util.Log;import com.growcn.mp3player01.Definition;import com.growcn.mp3player01.download.HttpDownloader;import com.growcn.mp3player01.model.*;public class DownloadService extends Service implements Definition{	static String TAG = "mp3player01";	@Override	public IBinder onBind(Intent arg0) {		// TODO Auto-generated method stub		return null;	}	@Override	public int onStartCommand(Intent intent, int flags, int startId) {		Mp3Info  mp3Info = (Mp3Info)intent.getSerializableExtra("mp3info");		//Log.i(TAG,"service-------"+mp3Info);		DownloadThread downloadThread=new DownloadThread(mp3Info);		Thread thread =new Thread(downloadThread);		thread.start();		return super.onStartCommand(intent, flags, startId);	}			class DownloadThread implements Runnable{		private Mp3Info mp3Info =null;				public DownloadThread(Mp3Info mp3Info) {			this.mp3Info = mp3Info;				}						@Override		public void run() {			String mp3Url = URL_DOWN_FILE+mp3Info.getMp3Name();			HttpDownloader httpDownloader = new HttpDownloader();			int result = httpDownloader.downFile(mp3Url,MY_ROOT+'/',mp3Info.getMp3Name());			String resultMessage = null;			if (result==-1){				resultMessage = "下载失败";			}else if (result==0){				resultMessage = "文件已经存在";							}else if(result == 1){				resultMessage = "下载成功";							}			//使用Notification提示客户下载结果		   Log.i(TAG,"resultMessage-------"+mp3Info.getMp3Name()+"---"+resultMessage);		}			}	}